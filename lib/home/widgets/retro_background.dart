import 'package:flutter/material.dart';

class RetroBackground extends StatelessWidget {
  const RetroBackground({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
